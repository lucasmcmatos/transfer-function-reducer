# transfer-function-reducer
This is a project from Control Engineering discipline. The software receives a tranfer function in block diagramas as input and process it automatically into a simplified transfer function or equivalent expression format.
